#include "Parser.h"

Node* Parser::parseHTML(const std::string& html) {
    // Placeholder: futuramente você vai implementar parsing real
    Node* root = new Node();
    root->tag = "div";
    root->text = "Hello Engine";

    return root;
}
